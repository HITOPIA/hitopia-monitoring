/node_modules
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo
.DS_Store
.vercel
npm-debug.log*
.omo

# Secrets — never commit (R8). Only .env.example is tracked.
.env
.env.*
!.env.example

# Prisma generated client (regenerated via postinstall / prisma generate)
/lib/generated
